# Scrapes the API's /metrics endpoint, which renders Prometheus text
# exposition directly -- no exporter sidecar needed.
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: groundtruth-rag
    metrics_path: /metrics
    static_configs:
      - targets: ["api:8000"]
